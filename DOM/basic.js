// What is the DOM?
// The Document Object Model (DOM) is a programming interface for web documents. It represents the structure of a webpage as a tree of objects, where each node is part of the document (like elements, attributes, text, etc.).

// Think of the DOM as a live, interactive representation of HTML that JavaScript can use to read, manipulate, or update the content, structure, and style of a webpage.

// Common DOM Properties & Methods

// ✅ Selection Methods:--- 

// document.getElementById(id) – Selects a single element by ID.

// document.getElementsByClassName(class) – Returns a collection of elements with the given class.

// document.getElementsByTagName(tag) – Returns a collection of elements by tag.

// document.querySelector(selector) – Selects the first element that matches a CSS selector.

// document.querySelectorAll(selector) – Selects all elements that match a CSS selector.

// 🔄 Manipulation Properties

// .innerHTML – Gets/sets the HTML content.

// .textContent – Gets/sets the text content.

// .style – Gets/sets inline CSS styles (e.g., element.style.color = "red").

// .classList – Allows you to add, remove, or toggle classes.

// 🎯 Attribute Methods:--

// .getAttribute(attrName) – Gets an attribute's value.

// .setAttribute(attrName, value) – Sets a new value for an attribute.

// .removeAttribute(attrName) – Removes an attribute.

// 🧱 Node Relationships:-----

// .parentNode – Gets the parent of a node.

// .childNodes – Gets all child nodes (including text/comments).

// .children – Gets child elements only.

// .firstChild / .lastChild

// .nextSibling / .previousSibling

// 📌 Event Handling

// .addEventListener(event, function) – Attaches an event handler to a node.

// .removeEventListener(event, function) – Removes an event handler.

// Common Events:
// click, mouseover, keydown, submit, change, scroll, load


// console.log(window.innerWidth);
// console.log(window.innerHeight);
// console.log(window.scrollX);
// console.log(window.screenY);
// console.log(window.location.href);
// console.log(window.location.hostname);
// console.log(window.location.pathname);



